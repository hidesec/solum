{
  "name": "solumjs",
  "version": "0.1.0",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "npm run build --workspace @solumjs/core && npm run build --workspace @solumjs/http && npm run build --workspace @solumjs/decorators && npm run build --workspace @solumjs/orm && npm run build --workspace @solumjs/database && npm run build --workspace @solumjs/cache && npm run build --workspace @solumjs/auth && npm run build --workspace @solumjs/events && npm run build --workspace @solumjs/schedule && npm run build --workspace @solumjs/validation && npm run build --workspace @solumjs/middlewares && npm run build --workspace @solumjs/config && npm run build --workspace backend-example",
    "test": "npm run test --workspace backend-example",
    "dev": "npm run dev --workspace backend-example",
    "migrate": "npm run migrate --workspace backend-example"
  }
}
