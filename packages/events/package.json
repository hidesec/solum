{
  "name": "@solumjs/events",
  "version": "0.1.0",
  "description": "SolumJS events: in-process event bus with @EventListener",
  "license": "MIT",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "prepublishOnly": "npm run build",
    "test": "jest"
  },
  "dependencies": {
    "@solumjs/core": "^0.1.0",
    "@solumjs/orm": "^0.1.0"
  },
  "devDependencies": {
    "@types/node": "^26.2.0",
    "typescript": "^6.0.3"
  }
}
